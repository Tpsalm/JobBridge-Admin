# JobBridge Admin Console

Standalone admin dashboard for the JobBridge platform.

## Usage

Open `https://tpsalm.github.io/JobBridge-Admin/` in your browser.

Sign in with a JobBridge account that has the `admin` role.
